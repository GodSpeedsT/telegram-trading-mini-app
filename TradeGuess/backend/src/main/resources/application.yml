spring:
  application:
    name: tradeguess-bot

  datasource:
    url: jdbc:postgresql://dpg-d50sg1vgi27c73asjij0-a.virginia-postgres.render.com:5432/tradeguess?sslmode=require
    username: tarantas
    password: IcBOKUbKRTrICPtCZSU4we90KY6HN38V
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: TradeGuessHikariPool

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 20
        format_sql: false
        default_schema: public
        order_inserts: true
        order_updates: true
    open-in-view: false

  data:
    redis:
      host: virginia-keyvalue.render.com
      port: 6379
      password: qmxF5ZWTOV7VzAlvnnNSJqHgd5aK31j1
      username: red-d50rtud6ubrc73a566ag
      ssl:
        enabled: true
      timeout: 5000ms
      repositories:
        enabled: false
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
        shutdown-timeout: 200ms

  cache:
    type: redis
    redis:
      time-to-live: 600000ms
      cache-null-values: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mvc:
    throw-exception-if-no-handler-found: true

  web:
    resources:
      add-mappings: false

  devtools:
    restart:
      enabled: false

token:
  signin:
    key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiYWRtaW4iOnRydWUsImlhdCI6MTUxNjIzOTAyMn0.KMUFsIDTnFmyG3nMiGM6H9FNFUROf3wh7SmqJp-QV30
    expire: 86400000

server:
  port: ${PORT:10000}
  servlet:
    context-path: /
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    min-response-size: 1024
  error:
    include-stacktrace: never
    include-message: always
  address: 0.0.0.0

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /manage
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true
  metrics:
    tags:
      application: tradeguess-bot

logging:
  level:
    root: INFO
    com.app.tradeguess: INFO
    org.springframework.web: WARN
    org.hibernate: ERROR
    com.zaxxer.hikari: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/application.log

cors:
  allowed-origins: https://web.telegram.org
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
  allowed-headers: "*"
  allow-credentials: true
  max-age: 3600

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
    username: ${TELEGRAM_BOT_USERNAME}
    path: /webhook/${TELEGRAM_BOT_TOKEN}
    allowed-updates: message,callback_query,chat_member

app:
  binance:
    enabled: ${BINANCE_ENABLED:false}
  rate-limit:
    enabled: true
    limit: 10
    window-size: 60s
  cache:
    user:
      ttl: 300000ms
    game:
      ttl: 600000ms